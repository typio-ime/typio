# Keyboard status salience

This page is retained for historical ADR links. Current keyboard status is an
owned `EngineMode` value. Its `ModeSalience` is encoded in Engine Protocol mode
records and controls whether the host automatically reveals a mode change.

Protocol compatibility follows the repository
[interface stability policy](../../../../docs/reference/stability.md), not the
retired binary ABI policy.
